# =============================================================================
# SOLANA MEME COIN EDUCATIONAL SYSTEM — EXAMPLE CONFIGURATION
# =============================================================================
# DISCLAIMER: This configuration is for educational and testing purposes only.
# Do NOT use on mainnet without full legal and compliance review.
# =============================================================================
# Copy this file to config.yaml and fill in your values.
# Never commit config.yaml with real private key paths to version control.

# ---------------------------------------------------------------------------
# Network settings
# ---------------------------------------------------------------------------
network:
  name: "mainnet"         # Change to "devnet" for safe testing
  rpc_url: "https://api.mainnet-beta.solana.com"   # Will be overridden based on name
  commitment: "confirmed"

rpc:
  endpoints:
    - https://api.mainnet-beta.solana.com  # Primary (mainnet)
    - https://rpc.ankr.com/solana          # Fallback (Ankr free tier)
  max_retries: 5
  retry_backoff_base: 1.5                 # Exponential backoff multiplier
  timeout_seconds: 30
  ws_endpoint: wss://api.mainnet-beta.solana.com  # WebSocket for subscriptions

# ---------------------------------------------------------------------------
# Wallet
# ---------------------------------------------------------------------------
wallet:
  keypair_path: ~/.config/solana/mainnet.json
  # Optional: pass as env var WALLET_KEYPAIR_PATH for security

# ---------------------------------------------------------------------------
# Monitoring / Detection settings
# ---------------------------------------------------------------------------
monitor:
  poll_interval: 60                       # Seconds between full scrape cycles
  enable_dexscreener: true
  enable_pumpfun: true
  enable_social: true                     # Twitter/X public search (Playwright)
  enable_onchain: true                    # WebSocket RPC log subscription

  dexscreener:
    base_url: https://api.dexscreener.com
    trending_endpoint: /latest/dex/tokens/recently-added  # Free endpoint
    chain: solana
    max_results: 50

  pumpfun:
    url: https://pump.fun
    scroll_rounds: 3
    load_wait_ms: 2000

  social:
    search_terms:
      - "solana meme coin launch"
      - "$SOL new token"
      - "just launched solana"
    results_per_term: 20
    min_mentions: 3

  onchain:
    raydium_amm_program: 675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8
    commitment: confirmed

# ---------------------------------------------------------------------------
# Filters — scoring thresholds to decide what gets queued
# ---------------------------------------------------------------------------
filters:
  min_liquidity_usd: 5000
  min_volume_5m: 500
  min_holders: 50
  max_age_hours: 2
  min_score: 60
  require_mint_revoked: false
  skip_high_rug_risk: true
  min_social_mentions: 2

# ---------------------------------------------------------------------------
# Analysis / Scoring weights (must sum to 1.0)
# ---------------------------------------------------------------------------
scoring:
  weights:
    volume_momentum: 0.30
    liquidity_depth: 0.20
    holder_growth: 0.20
    social_buzz: 0.15
    rug_risk_penalty: 0.15

# ---------------------------------------------------------------------------
# Token generation (for the educational "clone")
# ---------------------------------------------------------------------------
token_generation:
  name_strategy: append_suffix
  suffixes:
    - "2.0"
    - "Reloaded"
    - "Classic"
    - "OG"
    - "Genesis"
  default_supply: 1_000_000_000
  decimals: 6
  pool_supply_pct: 0.80
  retained_pct: 0.20

# ---------------------------------------------------------------------------
# Metadata hosting
# ---------------------------------------------------------------------------
metadata:
  provider: local_uri
  local_server_port: 8765

# ---------------------------------------------------------------------------
# Deployment
# ---------------------------------------------------------------------------
deploy:
  initial_sol: 0.1
  initial_token_pct: 1.0
  raydium_amm: 675kPX9MHTjS2zt1qfr1NYHuzeLXfQM9H24wFSUt1Mp8
  serum_dex: srmqPvymJeFKQ4zGQed1GFppgkRHL9kaELCbyksJtPX
  revoke_mint_authority: true
  revoke_freeze_authority: true
  revoke_update_authority: false
  slippage_bps: 100

# ---------------------------------------------------------------------------
# Approval gate (MUST be enabled)
# ---------------------------------------------------------------------------
approval:
  enabled: true
  timeout_seconds: 300
  confirm_phrase: "YES_MAINNET"

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
logging:
  level: INFO
  file: data/bot.log
  max_bytes: 10_485_760
  backup_count: 5
  format: "%(asctime)s [%(levelname)s] %(name)s: %(message)s"

# ---------------------------------------------------------------------------
# Dashboard
# ---------------------------------------------------------------------------
dashboard:
  port: 8501
  auto_refresh_seconds: 15
  max_detections_shown: 100
